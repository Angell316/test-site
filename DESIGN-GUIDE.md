# 🎨 Гайд по дизайну AnimeVerse

## Философия дизайна

Сайт создан в **современном loft-стиле** с акцентом на:
- Минимализм и чистоту
- Воздушность и пространство
- Технологичность и современность
- Элегантность без перегруженности

## 🎨 Цветовая схема

### Основные цвета

**Темные оттенки (Фон)**
- `dark-900`: #0a0a0a - Самый темный, основной фон
- `dark-800`: #131313 - Вторичный фон
- `dark-700`: #1a1a1a - Элементы на фоне
- `dark-600`: #242424 - Светлые элементы

**Фиолетовые акценты**
- `purple-primary`: #8b5cf6 - Основной акцент
- `purple-light`: #a78bfa - Светлый акцент (hover)
- `purple-dark`: #7c3aed - Темный акцент
- `purple-glow`: #c084fc - Свечение

**Нейтральные**
- Белый: #ffffff - Текст
- Серый 300: #d1d5db - Вторичный текст
- Серый 400: #9ca3af - Неактивные элементы

### Использование цветов

```jsx
// Фон страницы
className="bg-dark-900"

// Карточки и элементы
className="bg-dark-800"

// Кнопки и акценты
className="bg-purple-primary hover:bg-purple-light"

// Текст
className="text-white"           // Основной
className="text-gray-300"        // Вторичный
className="text-gray-400"        // Подсказки
```

## ✨ Ключевые эффекты

### 1. Glass Effect (Эффект стекла)

```jsx
// CSS класс
className="glass-effect"

// Что он делает:
{
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(40px);
  border: 1px solid rgba(255, 255, 255, 0.1);
}

// Где использовать:
- Навигационное меню
- Карточки
- Модальные окна
- Overlay элементы
```

### 2. Gradient Text (Градиентный текст)

```jsx
// CSS класс
className="gradient-text"

// Пример использования
<h1 className="gradient-text">
  AnimeVerse
</h1>

// Где использовать:
- Логотип
- Заголовки Hero секции
- Важные числа (статистика)
- Акцентные слова
```

### 3. Hover Lift (Подъем при наведении)

```jsx
// CSS класс
className="hover-lift"

// Что он делает:
{
  transition: all 0.3s;
  transform: translateY(-8px) on hover;
}

// Где использовать:
- Карточки аниме
- Кнопки
- Интерактивные элементы
```

### 4. Glow Effect (Свечение)

```jsx
// CSS класс
className="glow-on-hover"

// Или для кнопок
className="shadow-purple-glow hover:shadow-purple-glow-lg"

// Где использовать:
- Главные CTA кнопки
- Важные карточки
- Активные элементы
- Иконки в Header/Footer
```

## 🎭 Компоненты

### AnimeCard

**Особенности:**
- Пропорции 2:3 (стандарт для постеров)
- Градиентный overlay снизу
- Rating badge справа вверху
- Status badge слева вверху
- Play button появляется на hover
- Информация внизу: название, год, эпизоды, жанры

**Hover эффекты:**
- Изображение увеличивается (scale-110)
- Появляется кнопка Play
- Название меняет цвет на фиолетовый
- Карточка поднимается (hover-lift)
- Появляется фиолетовое свечение

### Header

**Особенности:**
- Fixed позиция (всегда сверху)
- Прозрачный по умолчанию
- При скролле: blur + темный фон
- Логотип слева с gradient box
- Навигация по центру (desktop)
- Поиск и мобильное меню справа

**Адаптивность:**
- Desktop: полная навигация
- Mobile: hamburger меню

### Hero Section

**Особенности:**
- Full screen height
- Градиентный фон с animated blur circles
- Grid pattern overlay
- Центрированный контент
- Badge сверху
- Большой gradient заголовок
- Описание
- 2 CTA кнопки
- Статистика внизу
- Scroll indicator

**Анимации:**
- Элементы появляются снизу (fadeInUp)
- С задержкой между элементами
- Плавающие blur circles

### Footer

**Структура:**
- Brand section (логотип + описание + соцсети)
- 3 колонки ссылок
- Bottom bar (copyright + made with love)

**Стиль:**
- Темный фон (dark-800)
- Разделитель сверху
- Glass эффекты на иконках соцсетей
- Hover эффекты на всех ссылках

## 📐 Spacing и Typography

### Отступы

```jsx
// Section padding
className="section-padding" // px-6 py-16 md:px-12 lg:px-24 lg:py-24

// Container
className="container-custom" // max-w-7xl mx-auto

// Spacing между элементами
space-y-4  // 1rem (16px)
space-y-6  // 1.5rem (24px)
space-y-8  // 2rem (32px)
space-y-12 // 3rem (48px)
```

### Типографика

```jsx
// Заголовки
className="text-4xl md:text-5xl lg:text-6xl font-display font-bold"

// Подзаголовки
className="text-2xl md:text-3xl font-display font-semibold"

// Основной текст
className="text-base text-gray-300"

// Большой текст
className="text-lg md:text-xl text-gray-300"

// Мелкий текст
className="text-sm text-gray-400"
```

### Шрифты

- **Display/Заголовки**: Poppins (font-display)
- **Основной текст**: Inter (по умолчанию)

## 🎬 Анимации

### fadeInUp

```jsx
className="animate-fadeInUp"

// С задержкой
className="animate-fadeInUp animation-delay-200"
className="animate-fadeInUp animation-delay-400"
className="animate-fadeInUp animation-delay-600"

// Или динамически
style={{ animationDelay: `${index * 100}ms` }}
```

### float

```jsx
className="animate-float"

// Использование:
// Для декоративных элементов
// Для иконок
// Для badge'ей
```

### pulse-glow

```jsx
className="animate-pulse-glow"

// Использование:
// Для важных кнопок
// Для уведомлений
// Для активных элементов
```

## 🔲 Grid Layouts

### Карточки аниме

```jsx
// Адаптивная сетка
className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-6 gap-6"

// Брейкпоинты:
// Mobile: 2 колонки
// Tablet: 3 колонки
// Desktop: 4-6 колонок
```

### Жанры

```jsx
className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-6 gap-4"
```

### Контент + Sidebar

```jsx
className="grid lg:grid-cols-3 gap-12"

// Sidebar: lg:col-span-1
// Content: lg:col-span-2
```

## 🎯 Кнопки

### Основная (Primary)

```jsx
<button className="px-8 py-4 rounded-xl bg-gradient-to-r from-purple-primary to-purple-dark text-white font-semibold shadow-purple-glow hover:shadow-purple-glow-lg transition-all duration-300 hover:scale-105">
  Начать просмотр
</button>
```

### Вторичная (Secondary)

```jsx
<button className="px-8 py-4 rounded-xl glass-effect hover:bg-white hover:bg-opacity-10 text-white font-semibold transition-all duration-300 hover:scale-105">
  Топ аниме
</button>
```

### Иконка

```jsx
<button className="p-2.5 rounded-lg glass-effect hover:bg-white hover:bg-opacity-10 transition-all duration-200 group">
  <Icon className="w-5 h-5 group-hover:text-purple-primary" />
</button>
```

## 🎨 Лучшие практики

### DO ✅

1. **Используйте glass-effect для overlay элементов**
2. **Добавляйте hover эффекты на интерактивные элементы**
3. **Используйте gradient-text для акцентов**
4. **Применяйте анимации fadeInUp для новых элементов**
5. **Используйте shadow-purple-glow для важных кнопок**
6. **Добавляйте transition-all для плавности**
7. **Используйте группы для hover эффектов на children**

### DON'T ❌

1. **Не перегружайте анимациями**
2. **Не используйте чистый черный (#000)**
3. **Не делайте слишком яркие цвета**
4. **Не забывайте про hover состояния**
5. **Не используйте резкие transitions**
6. **Не смешивайте разные стили кнопок**

## 🎪 Примеры использования

### Секция с заголовком

```jsx
<section className="section-padding bg-dark-900">
  <div className="container-custom">
    <div className="flex items-center space-x-4 mb-12">
      <div className="p-3 rounded-xl bg-gradient-to-br from-purple-primary to-purple-dark shadow-purple-glow">
        <Icon className="w-6 h-6 text-white" />
      </div>
      <div>
        <h2 className="text-3xl md:text-4xl font-display font-bold text-white">
          Заголовок
        </h2>
        <p className="text-gray-400 mt-1">
          Подзаголовок
        </p>
      </div>
    </div>
    {/* Контент */}
  </div>
</section>
```

### Карточка с glass эффектом

```jsx
<div className="p-6 rounded-2xl glass-effect hover:bg-white hover:bg-opacity-10 transition-all duration-300 hover-lift">
  <h3 className="text-xl font-semibold text-white mb-2">
    Заголовок
  </h3>
  <p className="text-gray-400">
    Описание
  </p>
</div>
```

---

**Следуйте этому гайду для поддержания единого стиля! 🎨**

